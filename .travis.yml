language: python

services:
  - docker
  

before_install:
  - docker build -t tsexton31/restapi .
  
  # Do what a docker compose file would

  - docker network create redisnet
  - docker ps
  - docker run --name redis --network redisnet redis
  - docker run -p 80:80 --network redisnet tsexton31/restapi
  #ensure ports are correct
  
script:
  - docker run tsexton31/websitetest:v1
  
after_success:
  - docker login -u $D_USERNAME -p $D_PASSWORD
  - docker push tsexton31/restapi

